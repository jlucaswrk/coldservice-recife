package com.coldservice.recife.ui.theme

import androidx.compose.ui.graphics.Color

// Cores principais do ColdService
val Primary = Color(0xFF6BB8E8)        // Azul claro
val PrimaryDark = Color(0xFF1A2744)    // Azul escuro
val Secondary = Color(0xFF25D366)      // Verde WhatsApp
val Accent = Color(0xFFFF6B35)         // Laranja urgência
val Warning = Color(0xFFF59E0B)        // Amarelo aviso

// Background escuro (tema dark)
val Background = Color(0xFF0A0F1A)
val Surface = Color(0xFF0D1526)
val SurfaceVariant = Color(0xFF1A2744)
val CardBackground = Color(0xFF131D35)

// Textos
val OnPrimary = Color.White
val OnBackground = Color.White
val OnSurface = Color.White
val TextSecondary = Color(0xFF9DD1F1)
val TextMuted = Color(0xFF6B7B8F)

// Status
val Online = Color(0xFF25D366)
val Offline = Color(0xFFF59E0B)
val Error = Color(0xFFEF4444)

// Borders
val Border = Color(0xFF243556)
val BorderLight = Color(0xFF34496D)
